{
  "name": "huntloop",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @huntloop/web",
    "build": "npm run build --workspace @huntloop/web",
    "start": "npm run start --workspace @huntloop/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "audit:site": "node scripts/audit.mjs",
    "verify": "npm run typecheck && npm run lint && npm test && npm run audit:site && npm run build"
  },
  "engines": {
    "node": ">=22.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "typescript-eslint": "^8.67.0"
  }
}
